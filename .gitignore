# Python
venv/
__pycache__/
*.pyc
*.model

# All built data is reproducible from scripts — regenerate with
#   python scripts/build_all.py
# so it's kept OUT of git (avoids GitHub's 100 MB/file limit + LFS hassle).
# To publish the dataset, use Hugging Face Datasets, not git.
data/
